const window = {
    w: 1400,
    h: 1000,
    c: "black",
};

const cellSet = {
    w: 100,
    h: 100,
    c: "green",
};

const wallSet = {
    h: 5,
    c: "orange",
}

const player = {
    radius: 20,
    w: 60,
    h: 30,
    headColor: "red",
    bodyColor: "blue",
};

const weapon = {
    w: 20,
    h: 30,
    color: "pink",
    statOnGround: 0,
    statInHand: 1, 
};


export {window, cellSet, wallSet, player, weapon}